<%= semantic_form_for @comment=Comment.new, url: topic_comments_path(@topic) do |f| %>
  <%=f.inputs do %>
    <%= f.input :content, input_html: {rows:3} %>
  <% end %>
  <%=f.action :submit, :as => :button %>
  </div>
<% end %>
